import { rem } from '@mantine/core';

export const THEME_TOGGLE_DEFAULTS = {
  buttonSize: 36,
  iconSize: 20,
  iconStroke: 1.5,
};

export const USE_ROOT_SHELL_DATA_DEFAULTS = {
  fixedAt: 120,
  maxWidth: 750,
};

export const CHAT_PLACEHOLDER_ICON_DEFAULTS = {
  style: { width: rem(96), height: rem(96) },
  stroke: 1.5,
  color: 'var(--mantine-color-blue-filled)',
};
